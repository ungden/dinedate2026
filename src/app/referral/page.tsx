import ReferralClient from '@/components/pages/ReferralClient';

export const dynamic = 'force-dynamic';

export const metadata = {
  title: 'Gioi thieu ban be - DineDate',
  description: 'Gioi thieu ban be va nhan thuong len den 50.000d moi nguoi',
};

export default function ReferralPage() {
  return <ReferralClient />;
}
